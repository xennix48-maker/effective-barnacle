# Web (Vite) — exposed to client via import.meta.env
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-KEY
VITE_BOT_USERNAME=BITCOIN_MININGMACHINE_BOT

# Supabase Edge Function secrets (set via `supabase secrets set`)
# BOT_TOKEN=                # from @BotFather (rotate the leaked one first)
# ADMIN_TG_IDS=8353748526   # your Telegram numeric ID; comma-separate for multiple admins
# SUPABASE_SERVICE_ROLE_KEY=
